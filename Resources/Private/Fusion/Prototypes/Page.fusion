prototype(Page) {
	head.photoswipe = PS:TagCSS
	body {
		javascripts {
			photoswipeMarkup = PS:Markup {
				@if.isLive = ${node.context.live}
				@position = 'start 1'
			}
			photoswipeJavascript = PS:TagJS {
				@if.isLive = ${node.context.live}
			}
		}
	}
}
