prototype(Page) {
	head.photoswipe = PhotoSwipe:TagCSS
	body {
		javascripts {
			photoswipeMarkup = PhotoSwipe:Markup {
				@if.isLive = ${node.context.live}
				@position = 'start 1'
			}
			photoswipeJavascript = PhotoSwipe:TagJS {
				@if.isLive = ${node.context.live}
			}
		}
	}
}
